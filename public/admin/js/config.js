var primary = localStorage.getItem("primary") || '#4466f2';
var secondary = localStorage.getItem("secondary") || '#1ea6ec';

window.endlessAdminConfig = {
	// Theme Primary ColorList
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
